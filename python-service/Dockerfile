FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .

# Create a cleaned requirements file without aioredis and comments
RUN grep -v '^#\|^$\|aioredis' requirements.txt > /tmp/clean_requirements.txt && \
    pip install --no-cache-dir -r /tmp/clean_requirements.txt

COPY . .

ENV PYTHONPATH=/app

EXPOSE 8000

CMD ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
